# Zigdexer

Baseline Zig terminal app scaffold.

## Run

```bash
zig build run
```

## Test

```bash
zig build test
```

## Project structure

- `/src/main.zig` entrypoint
- `/src/app.zig` app-level flow
- `/src/commands/help.zig` first command module for extensibility
